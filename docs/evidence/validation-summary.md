# Validation Summary

These are point-in-time validation results recorded during EventPulse development. They are not permanent availability or performance guarantees.

## Application

- Pytest: **31 passed**
- Coverage: **86.62%**
- Health endpoint: HTTP 200
- Readiness endpoint: HTTP 200
- Events endpoint: HTTP 200
- Booking tests include successful, invalid and concurrent capacity-protection behavior.

## CI And Security

- Ruff formatting and linting passed.
- Mypy passed.
- GitHub CI passed for the final observability validation fix.
- Gitleaks passed.
- Trivy filesystem scan passed with no blocking HIGH/CRITICAL findings.
- Trivy image scan passed with no blocking HIGH/CRITICAL findings.
- SonarQube quality gate was integrated for trusted main-branch analysis.

## Supply Chain

- GHCR image deployed by immutable digest.
- SBOM and GitHub provenance generated by the publishing workflow.
- Cosign keyless signing used for the published image.
- Kyverno accepted the signed EventPulse image.
- Kyverno rejected unsigned or untrusted images during policy validation.

## AWS And Kubernetes

- EventPulse Pods: **2 Ready** during validation.
- EKS worker nodes: Ready during validation.
- RDS PostgreSQL: private, encrypted and available during validation.
- ALB targets: healthy during validation.
- Terraform post-apply plans showed no changes where validated.

## Observability

- Prometheus EventPulse target: **UP** after deploying the image containing `/metrics`.
- Prometheus request metrics: present.
- Prometheus latency metrics: present.
- PrometheusRule: loaded.
- Grafana EventPulse dashboard: provisioned.
- Fluent Bit: healthy.
- CloudWatch log group and EventPulse log events: present.

## Resilience

- Controlled resilience test passed.
- The test deleted one API Pod, waited for replacement and rollout recovery, temporarily scaled to one replica, restored two replicas and confirmed API health.

## Notable Validation Correction

The observability validation initially caught that the live image did not expose `/metrics` and Prometheus marked the target down with HTTP 404. The fix was to publish and deploy a new signed image containing the metrics endpoint, then update validation to wait for target discovery and verify actual metric series.
